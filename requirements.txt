pandas>=2.0
numpy>=1.24
requests>=2.31
PyYAML>=6.0
# Official Upstox SDK - used for the V3 market-data websocket streamer.
# REST calls (auth, historical, orders, option chain) use plain `requests`.
upstox-python-sdk>=2.14.0
pytest>=7.4
